24年6月，gpt助手上线
25年2月，接Deepseek
25年2月，实现思考模式
25年3月，制度问答助手上线，支持多轮思考和工具调用
25年4月，接口由dify切换到自己的服务，修复删除按钮bug
25年5月，接SSO，三端
25年6月，非现场问答助手，取消回答功能，修复登录session过期导致的bug，代码精简优化
25年7月，纪委助手
25年8月，上线模型切换功能
25年9月，上线gpts功能
25年10月，
优化模型切换，done
  简化选择，增加友好提示
  选择模型之后，记住用户的选择，可cookie可服务端，下次新打开会话的时候，优先选择上次用户的选择的模型，如果该模型已经不存在了，再从可用模型里面找默认的那个
  每个会话记录最后使用的模型，当再次打开这个会话的时候，优先使用上次使用的模型，如果这个模型已经不存在了，再从可用模型里面找默认的那个
  注意，
    如果用户选择了A，如果没问问题，就关了页面，那应该不算，选择了A并且使用它问了问题，这时候才记录他选择了A
    如果用户选择了A，也问了问题，这时候他又打开了上一次会话，那个会话用的是B，用户接着使用B问了问题，然后用户再创建一个新会话，还是应该用上次选择使用的A
数据大屏原型，done
  apps/assistant-dashboard/index.html根据这个原型，帮我改造成一个前端项目，done
    用 TailwindCSS 做布局 → 容易响应式 + 调整比例
    用 Recharts / ECharts → 数据图表
    用 React Query + Axios 拉数据
    支持自动刷新或 WebSocket 推送
  根据apps/assistant-dashboard这个大屏，创建一个后端项目，放在servers/assistant-metrics-api下，是一个python项目，done
    参考assistant-bff的框架和部署方式
    把前端mock的数据迁移到后端，后端提供接口
  根据这个大屏原型，把前端和后端接口连接起来
模型选择增加VL选项 done
模型选择，点击了，出了一个弹窗，没有选择，这时鼠标在其它地方点击了之后，这个弹窗应该消失，但没有，done
优化数据大屏布局，清晰每一块数据含义，并且手动优化了样式，done

大屏数据实现
  根据assistant-metrics-api这个大屏数据接口，研究一下assistant-bff这个里面的业务逻辑，
  你看看要怎样把这个数据接口给变更真实可用的
  先说说你的思路是啥
  。。。
  按你的思路实现一版
ChatGemini 核心指标总览页面
  ChatGemini 改成 GPT助手
  活跃用户数卡片去掉
  累计用户数改成用户数
  当前总请求数改成请求数
  用户数卡片里的过去 7 日新增去掉，改成与上一期的同比增长了多少还是下降了多少
  请求数卡片里的今日新增去掉，改成与上一期的同比增长了多少还是下降了多少
  请求趋势卡片上的过去14天这个span去掉
  页面右上角的卡片，数据更新几秒前这个去掉
  页面右上角的卡片，时间筛选增加一个今天选项，默认还是过去14天
  页面右上角的卡片，时间筛选增加一个所有时间选项
  时间选择后可以保存到session里面，刷新页面后还是选择后的选项
  现在这个时间选择可能是一个假的，没用的，改成真实生效的
  用户数卡片右边增加一个会话数卡片，
解决VPN影响
  我的http://0.0.0.0:5010这个服务里面会请求http://0.0.0.0:5008这个服务，
  我开了一个vpn，这个请求会受vpn的影响不能正常响应，把vpn关了就行了
  我浏览器请求我自己启动的服务localhost:3000和localhost:4173就都不受vpn影响
  能不能让这个请求也不受vpn影响？
时间范围筛选那里效果有一些差，有一个椭圆，很不和谐，换成方框又大框套小框，也不好看，我尝试去掉框框，字体又变大了，也不好看，你看看怎么帮我调整一下



上线一版，
1是验证除大屏之外的功能，并且最好能稳定运行一周以上
2是验证大屏的功能，看看数据是否能够正常工作


model_config.py移到.env里


add remote addr
git remote add modelscope https://oauth2:ms-151987bf-ae27-441c-8686-f13aec57a5df@www.modelscope.cn/eason5870/fxcjg.git
first push
git push --set-upstream modelscope main:refs/for/github
normal push
git push modelscope master:github-master


test