# Changelog

## 版本维护规范
- Changelog 以变更日期为主标题；若该日期对应 Sidebar 中的正式发布记录，则在日期后附加版本号。
- 普通功能调整和问题修复只记录日期，不要求分配版本号；Sidebar 仅维护面向用户的正式发布版本。
- 小的交互优化或修复类改动升级第三位，例如 `v1.0.1` -> `v1.0.2`。
- 小的新功能添加升级第二位，例如 `v1.0.1` -> `v1.1.0`。
- 更大的升级、架构级调整或产品级变化升级第一位，例如 `v1.0.1` -> `v2.0.0`。
- 正式发布时应同步更新 Sidebar 当前版本、Sidebar 发布记录和本文件中的版本标记。


## [2026.07.29] LLM Platform 按 Space 自助管理 API Key
- **默认空间无感使用**：主集群所在 Space 对所有个人和项目默认开放；只有获得额外 Space 后，页面才展示空间列表、Key 所属空间和创建选择。
- **撤权即吊销**：管理员取消额外 Space 关联时，对应 API Key 从有效配置永久移除，用户端不再展示，重新授权也不会恢复旧 Key。
- **旧 Key 安全归位**：旧 Key 的全部 Site 能唯一映射到主集群默认 Space 时自动迁移并直接显示默认 Space；跨 Space 或无法判断的 Key 继续保留兼容范围提示。
- **创建上下文收敛**：个人和项目 API Key 创建时必须选择已授权且当前可用的服务空间，不再向用户暴露 Site 选择。
- **权限双重校验**：assistant-bff 校验登录用户和项目 Owner 身份，portal-backend 继续校验 Space 资格与有效服务状态。
- **管理闭环**：Key 列表展示所属 Space，并支持备注、启停和吊销；旧 Key 继续以兼容范围标识展示。
- **自动生效**：新 Key 保存 `space_id`，后续 Site 投影和 Agent 同步由 Access Revision 自动完成。

## [2026.07.20] 版本更新未读提示
- **三级提醒**：新增账号菜单、当前版本和具体版本条目的逐级小红点；版本条目实际进入可视区域后自动标记已读。
- **账号级同步**：已读阶段保存到服务端账号，清理浏览器缓存、切换设备或重复登录不会重新出现已经看过的版本提醒。
- **历史降噪**：只有显式标记为通知版本的发布记录参与提醒，既有历史版本默认已读。

## [2026.07.20] [v1.4.0] 智能办公试用上线
- **智能办公里程碑**：智能办公开启定向试用，首批受邀用户可在 AI 助手中体验一体化智能问答与办公协作。
## [2026.07.20] [v1.3.2] 个人资料库试用上线
- **个人资料库里程碑**：个人资料库开启定向试用，首批受邀用户可集中管理工作资料，为后续知识化应用做好准备。
## [2026.07.16] 智能办公白名单接入框架
- **双产品模式**：左侧品牌区新增受权限控制的“智能问答 / 智能办公”切换 Tab；进入智能办公后，原菜单替换为外部菜单框架，右侧切换为 iframe 嵌入容器，切回时保留原助手页面和会话状态。
- **白名单试用**：智能办公默认关闭且名单为空，非白名单用户不显示入口、不挂载 iframe，原有页面结构与交互保持不变。
- **双接口分层**：新增智能办公权限与 bootstrap 接口，普通权限查询只返回是否获准，标题、菜单和 iframe 地址仅向获准用户返回。
- **安全兜底**：外部地址未配置时展示待接入占位页，非法 iframe URL scheme 会被后端过滤，手工构造的外部模式 URL 不能绕过服务端门控。
- **界面收敛**：智能办公顶栏移除“重新加载”和“白名单试用”提示，左侧用户区同步去掉试用标签；加载失败场景仍保留重试入口。
- **菜单后台化**：智能办公基础路径和菜单 JSON 复用产品配置保存；管理员可配置 `/b/` 与相对 path，后端在 bootstrap 时安全拼成 iframe 地址，无需新增数据表或重启服务。

## [2026.07.16] 智能办公路径式接入
- **访问路径**：智能办公菜单改用与外部菜单 `path` 对应的 `/chat/deer/<menu-path>` 作为主系统可分享、可刷新的地址，支持多级路径；旧查询参数链接会自动迁移。
- **iframe 映射**：支持将同域主系统路径映射至反向代理后的外部办公系统路径，例如 `/chat/deer/new` 加载 `/deer/new`。

## [2026.06.26] 智能体默认可见性改为仅自己
- **创建默认值收紧**：更多智能体里的“创建智能体”默认权限从“所有人可见”改为“仅自己可见”，降低误建成全员可见的概率。
- **后端兜底一致**：即使前端未来漏传 `auth`，后端创建和更新接口也会默认按 `self` 处理，避免默认权限回退不一致。
## [2026.06.24] 智能体菜单与默认置顶语义收敛
- **菜单展示收口**：左侧智能体菜单改为严格基于用户的智能体可见权限生成，修复普通用户在没有“更多智能体”入口时仍看到重复 ECharts / PPT 助手的问题。
- **默认置顶语义统一**：对用户可见的智能体默认都出现在左侧菜单；拥有“更多智能体”入口的用户仍可单独取消或恢复置顶，但这只影响菜单展示，不影响访问权限。
- **制度助手去特判**：制度助手不再依赖代码中的强制置顶逻辑，是否全员可见或白名单可见统一由智能体配置控制，和其他系统/自定义智能体保持一致。
## [2026.06.22] [v1.3.0] 智能体资料使用反馈
- **资料来源弱反馈**：智能体聊天回答下方增加轻量状态文案，提示本轮是否参考了上传文件、知识库，帮助使用者快速判断资料是否真正参与了回答。
- **读取失败弱提示**：当附件或知识库读取失败时，会在回答附近显示克制的失败提示，避免资料调用失败后界面完全沉默。
- **历史兼容保留**：资料使用状态随会话消息一起保存，刷新或重新打开历史会话时仍可继续看到对应反馈。
## [2026.06.22] 智能体能力子项只读展示
- **父级能力唯一入口**：创建和编辑智能体页保留“会话附件”和“知识库”两个父级能力开关，子项不再作为可操作配置入口。
- **子项改为说明层**：查看/读取两个底层工具继续展示在父级能力下方，但仅用于解释该能力实际包含的工具集合，避免再次暴露底层配置复杂度。
## [2026.06.22] 智能体能力子项联动
- **分组下钻展示**：在“会话附件”和“知识库”两个能力组下展示具体的查看/读取子能力，让用户能看清大项实际包含的底层工具。
- **联动与依赖**：勾选父级能力会同步勾选全部子项；子项支持单独调整，父级会根据状态展示全选或部分选中。读取子能力依赖对应的查看子能力，避免出现难以理解的配置组合。
## [2026.06.22] 智能体能力配置收敛
- **产品级开关**：创建和编辑智能体页将“会话附件”和“知识库”改成两个更贴近用户心智的资料来源开关，不再要求用户理解底层的查看/读取动作拆分。
- **底层协议不变**：前端继续把两个开关映射到原有 capability id 提交，后端权限边界、运行时能力注册和历史数据兼容性保持不变。
- **未来能力兼容**：如果后端后续新增了不属于这两个分组的能力，页面仍会继续以单独卡片展示，避免新能力被前端重构隐藏。
## [2026.06.22] 智能体能力说明国际化
- **能力说明本地化**：创建和编辑智能体页中的四个可用能力说明改为走前端中英文语言包，不再直接显示后端返回的英文描述。
- **兼容未知能力**：页面对已知能力使用本地化文案，对未来新增但前端尚未适配的能力继续回退展示后端描述，避免页面空白或丢失说明。
## [2026.06.22] 智能体级上传类型
- **策略归属调整**：将聊天附件“允许上传类型”从后台模型配置迁移为智能体配置，创建和编辑智能体时可直接控制允许上传文档、图片或两者。
- **模型能力语义收口**：模型配置继续只保留 `supports_native_image_input` 这类真实能力，用于决定是直接传入图片还是通过附件读取/OCR 回退。
- **上传校验兼容**：会话附件上传改为优先读取智能体 `upload_file_types`；智能体知识库和资料库上传继续限制为文档类型，避免现有知识读取链路被图片输入语义污染。
## [2026.06.21] 部署范围可选
- **兼容原有调用**：`deploy.sh <环境>` 继续执行全部后端和前端部署。
- **按范围部署**：新增全部后端、全部前端、指定后端和指定前端参数，且支持组合、重复指定及服务名称校验。

## [2026.06.21] Agent Runtime v3 与能力配置
- **新智能体默认 v3**：页面新创建的智能体固定使用 `handler_key=agent_runtime_v3`；已有智能体和系统助手保留原执行器，支持平滑升级与回退。
- **能力配置产品链路**：新增可用能力目录接口，创建/编辑页可选择会话附件和智能体知识的查看、读取能力，配置保存到 `enabled_capabilities`，运行时只向模型提供已选择且已授权的工具。
- **知识与附件隔离**：智能体知识使用独立 `knowledge_list`、`knowledge_read_text` 工具和授权作用域，不再混入会话附件语义。
- **运行时治理**：v3 支持 Run、Step、ActionCall 状态追踪，参数校验、权限复核、工具超时、调用预算、结构化错误和有界内存记录。
- **高风险确认**：写入及高风险能力返回 `CONFIRMATION_REQUIRED`，前端确认后使用服务端签名、绑定用户与参数、短期有效的令牌安全重试。

## [2026.06.19] 全站深色与浅色模式支持
- **系统偏好设置主题切换**：支持浅色模式、深色模式及系统偏好自动同步，将设置状态持久化保存至 `localStorage`。
- **智能体各个工作区深色适配**：深度重构了智能体广场（Gpts）、我的智能体（MyGpts）及创建智能体（CreateGpt）工作区的暗色样式，解决智能体卡片底色刺白和图标可见度问题。
- **管理员配置界面视觉优化**：将管理员配置中的表单面板、表格头、警告区及大量的状态徽标（成功、失败、待处理）进行深色主题适配，彻底修复了白色边框和硬编码暗色字体导致的信息难辨认缺陷。
- **数据看板同步**：为 assistant-dashboard 引入完全相同的“主题模式”切换菜单，并使用全局 CSS 变量与 Tailwind 类拦截技术实现数据看板的浅色与深色主题完美展示，自适应调整 Recharts 图表及其 tooltip 的暗色表现。
- **修复 TS 编译问题**：修复了 CreateGpt 隐式 any 警告及 MyGpts 中错误的 `sendUserAlert` 参数调用。

## [2026.06.19] 智能体广场顶部间距与触发器对齐
- **顶栏内边距统一**：统一 React 顶栏与原型顶栏 padding 样式，由不合规的 Tailwind `px-6.5`（实际解析为0导致文字贴左）和 breakpoints 修复为 `px-[26px] max-[900px]:px-4`，对齐主聊天页 Header 间距，解决无边栏/折叠时文字贴左的视觉缺陷。
- **触发器按钮逻辑**：修正 workspaces 中 Topbar 菜单按钮的显示断点，并在 App.tsx 中补齐 onToggleSidebar 传递，确保在折叠与移动端下正常唤出侧栏。
- **静态原型同步**：同步更新 `assistai-ui.js` 顶栏结构、类名及发布记录，修复点击智能体广场等子页面导致的样式差异。

## [2026.06.19] 静态原型 CSS 彻底重构成 Tailwind
- **Play CDN 动态运行时**：移除了所有静态 Vanilla CSS 文件，转为在浏览器中通过 Tailwind Play CDN 进行实时解析渲染，确保继续保持“无构建双击打开”的静态原型流程。
- **布局与样式翻译**：将主框架 shell、侧边栏、对话流、各工作台 HTML 模板中的类名替换为等价的 Tailwind CSS 实用类，并继续保留原 Class 作为 DOM 选择器基础。
- **全局状态样式统一**：将滚动条、过渡以及基于 class 切换的状态控制统一归口至每个页面的 `<style>` 标签，确保 prototype 与生产 React 应用在代码结构与样式层面高度对齐。
- **用户偏好菜单与子页面迁移**：在静态原型中补齐并打通了用户菜单中“当前版本”、“系统设置”模态弹窗，以及“管理员配置”与“语音测试”的模块化工作区迁移。
  - **功能变更与偏好设置模态框**：实现了完全由数据驱动的变更日志弹窗（内置 v1.0.1 至 v1.6.2 的日志详情）及支持中英文切换演示的个性化设置弹窗。
  - **管理员后台与语音实验室**：以独立模块 `admin-workspace.js` 和 `voice-lab-workspace.js` 实现了后台管理功能与语音测试体验。管理员页支持模型增删改、智能体总开关/白名单配置、权限分配与审计日志追踪；语音测试页打通浏览器真实 Web Audio API 麦克风录制回放与 TTS 朗读引擎。

- **纯 Tailwind 状态化迁移 (方案 2)**：
  - **框架层对齐 (第一步)**：将侧边栏收起、展开触发按钮、移动端侧滑遮罩的状态显隐切换，全部用 Tailwind 的 `group`、`peer` 及对应的类条件选择器（如 `group-[.is-sidebar-hidden]:!inline-grid` 等）重新编写，不再依赖原生 CSS 覆盖。
  - **对话与控制层对齐 (第二步)**：对聊天区空状态（`is-empty`）、工作台切换（`is-workspace`）、文件上传进度、历史会话折叠（`is-collapsed`）以及模型选择菜单显隐状态进行纯 Tailwind 化重构，把状态显隐规则全面归口至 JS 模板原子类。
  - **精简内联 CSS**：完全从各页面的 `<style>` 区块中移除了上述框架、侧边栏、对话交互相关的数十条原生 CSS 规则，使原型代码在实现细节上高度贴近生产环境的 React 原子类写法。


## [2026.06.18] 智能体可见人员后台化
- **后台可配可见范围**：管理员配置页新增智能体入口可见性配置，可直接切换“所有登录用户可见”或“仅指定账号可见”。
- **名单结构化存储**：智能体可见人员名单改为存储在管理员配置中，支持邮箱或 `sub` 标识，避免继续依赖环境变量白名单。
- **权限语义拆分**：`gpts.manage` 仅表示管理智能体的能力，不再隐含“更多智能体”入口可见；运行时优先读取后台可见性配置，未配置时兼容旧 `GPTS_WHITE_LIST`。

## [2026.06.18] LLM Platform API Keys 分区展示
- **个人与项目分区**：LLM Platform 的 API Keys 页面改为先展示个人 API Keys，再按项目分别展示项目 API Keys，避免不同归属的密钥混在同一张表里。
- **创建入口就近放置**：个人 Token 和每个项目 Token 的创建按钮放回各自区块，减少创建时误把 Token 建到错误归属下的概率。
- **异常项目兜底**：若后端返回了当前项目列表外的项目 Token，页面会单独标记为异常项目区块展示，避免数据静默丢失。

## [2026.06.18] 智能体广场页顶部样式对齐
- **顶栏风格统一**：更多智能体页面顶部区从圆角卡片改为更接近原型 `assistai-ui/gpts.html` 的轻薄半透明 topbar 视觉。
- **字体与配色收口**：统一标题区字重、字距、边线和青灰色层级，使 header 与 AssistAI 原型的顶部栏语气保持一致。
- **操作按钮收轻**：`我的智能体` 和 `创建智能体` 按钮改为更贴近原型的浅边框、浅强调按钮样式，减少与顶部栏风格冲突。

## [2026.06.18] 智能体配置页布局优化
- **单栏配置**：创建/编辑智能体页面暂时合并为居中的单栏配置流，保留外层双栏框架，为后续右侧预览区域预留结构。
- **知识库列表滚动**：智能体知识库文件较多时改为列表内部滚动，避免页面被文件列表无限拉长。
- **返回语义修正**：创建智能体未保存时返回更多智能体广场，编辑已有智能体时返回我的智能体；我的智能体页补充返回广场入口。

## [2026.06.17] 导入智能体所有者兼容修复
- **邮箱 owner 兼容**：修复部分从旧数据导入到 PostgreSQL 的智能体将 `owner` 保存为邮箱时，编辑和删除接口仍只按 `sub` 校验，导致页面提示“当前账号不是所有者”的问题。
- **无需手工迁移**：所有者判定现统一兼容当前账号的邮箱和用户唯一标识，已有导入数据无需手工改库即可继续转让、编辑和删除。

## [2026.06.15] MinIO 多 endpoint 容灾
- **多地址配置**：`MINIO_ENDPOINT` 现在支持用英文逗号或分号配置多个 `host:port`。
- **自动切换**：对象存储层会优先复用当前可用 endpoint；连接或请求失败时会自动尝试后续 endpoint。
- **诊断增强**：依赖健康检查会返回已配置 endpoint 列表和当前活跃 endpoint，便于排查存储切换状态。

## [2026.06.13] 智能体与附件端内隔离
- **默认端内隔离**：普通附件和默认创建的智能体都按登录端隔离，避免不同入口之间的数据互通。
- **全局智能体例外**：审核通过的全局智能体可以跨端可见，知识文件跟随智能体作用域管理。
- **上传配额按端计算**：文件上传预约与重复内容复用都按用户 + 登录端维度计算，避免跨端绕过配额和审核边界。
- **会话清理一致**：删除会话时仅清理由当前登录端绑定的 session attachment，不影响其他端的文件引用。

## [2026.06.13] 附件过期时长可配置
- **过期时长可配**：会话附件自动过期时长改为读取 `FILE_LIFETIME_DAYS`，默认 7 天。
- **关闭自动过期**：将 `FILE_LIFETIME_DAYS` 设为 `0` 时，后台过期清理会直接跳过，文件不会被自动清理。

## [2026.06.13] 附件内容去重
- **同用户内容去重**：上传时流式计算 SHA-256；同一用户重复上传完全相同的文件时创建新的逻辑附件记录，但复用已有对象，不再重复写入 MinIO。
- **内容寻址对象键**：新上传对象统一保存到 `assistant-files/blobs/sha256/<完整文件 SHA-256>`，对象路径不依赖文件名、上传日期或用户标识，为后续全局去重策略预留空间。
- **统一生命周期**：会话附件和智能体知识文件共用引用安全删除逻辑；删除、会话清理和过期清理仅在对象没有其他逻辑引用时删除底层文件。
- **兼容旧附件**：历史附件继续使用原对象键，无需上线前批量迁移；新 schema 自动增加内容哈希字段和对象引用索引。
- **迁移字段保留**：SQLite 到 Postgres 的附件迁移同步保留内容哈希、用途和会话绑定字段。

## [2026.06.12] 聊天流式兼容修复
- **迁移状态按节点隔离**：`sqlite_migration_state` 改为按 `(SQLITE_MIGRATION_NODE_ID, source_path)` 记录，避免多节点共用同一路径的本地 sqlite 时互相覆盖迁移状态并触发重复导入。
- **流式兼容修复**：修复部分智能体在生产环境中虽然已收到 `chat-messages` 流式响应，但页面仍显示空白的问题。
- **协议自动识别**：前端聊天流解析不再按助手类型预判协议，而是按事件内容自动兼容旧版 `message/message_end` 和新版 kernel 事件流。
- **状态写入修复**：修复自定义助手会话中把 `conversation_id` 误写入 AI 状态而不是会话映射的问题，避免后续会话状态被污染。

## [2026.06.10] 授权智能体访问兼容
- **直接授权可访问**：被单个智能体授权的用户现在可以直接访问对应智能体详情，即使未具备更多智能体广场权限。
- **广场权限不变**：智能体广场入口仍由总开关和白名单控制，不会因为单个智能体授权而自动开放。

## [2026.06.10] 智能体术语统一
- **产品命名统一**：创建、探索、管理和侧边栏入口中的 GPT、GPTs 与专项助手统一命名为“智能体”。
- **概念层级清晰**：主通用聊天入口继续称为“AI 助手”，用户创建的专项能力统一称为“智能体”。
- **内部兼容不变**：API 路径、配置键、权限码和环境变量继续保留 `gpt/gpts` 命名，不影响现有部署。

## [2026.06.09] 智能体首选模型
- **创建时选模**：创建和编辑智能体时可以从当前用户可用的模型中选择首选模型。
- **模型下线降级**：首选模型下线、停用或对当前用户不可见时，聊天自动切换到全局默认模型，再降级到首个可用模型，避免智能体不可用。
- **失效配置提示**：编辑智能体时保留已下线的原首选模型并明确标记，方便创建者选择当前模型后更新配置。

## [2026.06.08] 智能体页面视觉统一
- **整体视觉统一**：重新设计更多智能体和创建智能体页面，沿用主聊天助手的浅灰蓝背景、青色强调、柔和边框与阴影层级。
- **智能体探索体验**：更多智能体页新增工作台式导览头部、轻量智能体卡片、分区说明和空状态。
- **配置工作台**：创建 GPT 页面改为主配置与侧边配置分栏布局，优化系统提示词、示例问题、知识文件和权限设置的层级与交互。

## [2026.06.07] GPT 长期知识文件
- **知识文件管理**：创建 GPT 后可继续上传助手全局长期知识文件，编辑 GPT 时可查看和删除已有知识文件。
- **文件用途分层**：文件映射新增助手知识文件与会话临时附件用途，二者均按助手 `gid` 归属，并预留会话标识。
- **按需工具读取**：自定义 GPT 聊天接入文档工具调用链，模型需要资料时再列出和读取知识文件，不默认注入全部文件正文。
- **生命周期隔离**：七天过期清理仅处理会话临时附件，助手知识文件仅在主动删除或删除 GPT 时清理。
- **权限隔离**：只有具备 GPT 管理权限的 GPT 所有者可以上传、查看和删除助手知识文件；聊天工具仅能访问当前助手的知识文件和本轮附件。

## [2026.06.07] 管理员模型配置与文件上传安全修复
- **新增模型修复**：管理员新增的已启用模型会加入主助手候选模型，再按默认可见模型和用户可见范围过滤，解决配置后聊天页仍不显示的问题。
- **状态与排序修复**：模型配置中的启用状态和排序现在会正确应用到主助手模型列表。
- **文件归属隔离**：上传文件记录绑定当前用户，文件下载、文件名查询、文本提取和聊天附件读取统一校验归属，阻止跨用户通过文件 ID 访问。
- **上传配额隔离**：上传文件数量新增按用户活动文件配额，避免单个用户耗尽全局额度，同时保留系统总容量保护。
- **上传失败清理**：文件对象保存成功但映射记录写入失败时会自动删除对象，减少无法访问和无法正常清理的孤儿文件。
- **错误信息收敛**：上传异常对外统一返回通用错误信息，底层存储路径和内部异常仅保留在服务端日志。
- **上传内存保护**：上传文件改为分块读取并在读取过程中执行大小限制，超限后立即停止，降低并发大文件占用内存的风险。
- **聊天附件限制**：单次聊天最多携带 10 个附件，自动文本提取默认最多注入 100,000 字符，前后端均增加数量拦截。
- **解析阻塞保护**：文档解析增加并发限制与 60 秒超时，降低复杂文件同时解析对聊天服务的影响。
- **Office 解压保护**：DOCX、XLSX 和 PPTX 上传时检查压缩条目数、总解压大小和压缩比，拒绝疑似解压炸弹文件。
- **附件总量限制**：单次聊天附件除数量限制外，新增默认 30MB 总字节数限制，降低多图片加载和 Base64 编码造成的内存压力。
- **解析进程隔离**：文档解析改为独立子进程执行，超过 60 秒后直接终止并回收进程，避免恶意文件长期占满解析线程。
- **上传配额预留**：上传开始前通过数据库事务原子预留配额，并统计进行中的上传，降低并发请求同时通过配额检查的问题。
- **清理失败保护**：过期文件对象删除失败时保留数据库映射，等待后续清理任务重试，避免产生失去追踪的孤儿文件。
- **文件签名校验**：PDF、Office、旧 DOC、图片和文本文件增加基础内容签名检查，拒绝与扩展名明显不匹配的伪装文件。
- **遗留路由移除**：移除未使用且缺少资源级授权的旧路径文件下载接口，统一通过文件 ID 和归属校验访问附件。
- **上传入口限流**：在 multipart 解析前限制上传请求体总大小，覆盖 Content-Length 和分块请求，降低超大请求占满临时磁盘的风险。
- **附件工具防放大**：附件工具对重复文件 ID 去重、校验参数类型并限制文本输出上限，聊天附件 ID 字段同时增加长度保护。
- **清理任务容错**：过期文件清理兼容旧的无时区时间戳，单个文件清理失败不再中断其余文件的清理。
- **取消上传清理**：客户端在对象存储写入期间断开时，等待后台写入结束并删除已写对象，降低取消请求产生孤儿文件的风险。
- **MinIO 缓存完整性**：远端文件先下载到独立临时文件，完成后原子发布；失败或大小不匹配的缓存会清理并重新下载。
- **附件内容收敛**：下载 MIME 类型改为由受支持扩展名确定，附件展示文件名移除控制字符，工具结果不再暴露服务器本地路径。
- **附件解析防放大**：聊天附件 ID 在归属校验后统一去重，文本字符预算下推到各格式提取循环，减少重复解析和先构造完整输出的内存风险。
- **上传配置校验**：后端拒绝未知或已禁用模型的上传请求，并执行全局上传开关及管理员模型上传类型配置，避免伪造模型 ID 回退到宽松规则。
- **跨节点清理互斥**：过期文件清理增加 Postgres advisory lock；SQLite 部署使用本机文件锁，避免多进程或多节点重复清理同一附件。
- **迁移指纹增强**：SQLite 到 Postgres 的迁移状态增加源文件 SHA-256 指纹，避免不同节点同路径、同大小和同时间戳的旧库被误判为已迁移。
- **遗留附件告警**：依赖健康检查会报告缺少用户归属信息的旧附件映射，便于上线后识别无法按新权限规则访问的遗留数据。
- **上传字段边界**：上传文件名会移除浏览器伪路径并拒绝控制字符、超长名称和异常模型 ID，降低日志、响应头及配置查询异常风险。
- **MinIO 缓存校验**：缓存命中会刷新最近使用时间；远端下载完成后再次核对实际字节数，异常内容不会发布为有效缓存。

## [2026.06.05] 流式内存优化
- **流式内存优化**：聊天流式输出期间合并高频会话状态更新，并移除重复 busy 状态写入，降低长回答时 Redux 更新、页面重渲染和浏览器内存压力。

## [2026.06.04] 会话历史迁移与隔离优化
- **迁移性能修复**：旧浏览器本地历史导入改为前端分批后台上传、后端按小批次在线程池写库，避免大历史用户首次打开页面时被导入请求阻塞登录和历史加载。
- **历史附件降级**：旧历史中的附件如果尚未迁移文件映射或文件本体，页面会明确显示为“文件暂不可用”，不再展示为未命名的可点击附件或继续触发无效预览请求。
- **本地业务数据迁移**：Postgres 模式启动前会自动从本机旧 SQLite 迁移会话历史、`file_mapping`、`custom_gpts`、`user_gpts_state` 和 `user_config_version`，并兼容旧版 `pins.db` 的附件映射结构。
- **多端历史隔离**：会话历史元信息新增登录端 Provider 标识，同一用户在不同登录端查看历史时会按 Provider 隔离，避免多端共用一套后端时历史互相串出。

## [2026.06.03] [v1.2.1] 默认中文与语言切换
- **默认语言**：前端 fallback 语言调整为简体中文，未保存过语言偏好的用户默认进入中文界面。
- **系统设置**：账号菜单新增系统设置入口，语言切换已收敛到设置弹窗中，并为后续配色、记忆等偏好配置预留位置。
- **GPTS 总览**：管理员后台新增 `/admin/gpts` 模块，集中展示 GPTS 总开关、生效范围、白名单用户、显式管理用户和白名单 fallback 管理用户。
- **规则解释**：页面直接给出“所有登录用户可见”或“仅白名单用户可见”的最终判定，并展示当前账号是否可见、是否可管理。
- **兼容提示**：总览页明确提示 `GPTS_WHITE_LIST` 用户当前会自动获得 `admin.access` 和 `gpts.manage` fallback 权限，降低管理员误解配置来源的概率。
- **配置校验**：Postgres 模式下会在启动配置校验阶段检查 `SESSION_HISTORY_ENCRYPTION_KEY` 是否为合法 Fernet key，避免运行中才暴露历史加解密异常。

## [2026.06.02] [v1.2.0] 管理员配置页
- **入口新增**：左下角账号菜单新增管理员入口，仅对具备管理员权限的账号显示。
- **页面拆分**：新增独立 `/admin` 页面，避免将后台配置能力直接混入聊天主界面。
- **配置维护**：管理员页支持维护模型能力、权限分配和功能开关，覆盖最核心的业务配置域。
- **操作闭环**：新增、编辑、删除流程已在页面内打通，便于上线前逐步替代散落的业务型环境变量。
- **审计补充**：新增管理员操作审计记录，页面可直接查看最近的配置变更。
- **配置接管**：`gpts_feature_enabled` 和 `voice_lab.access` 已开始支持从管理员配置生效，并保留环境变量 fallback。
- **权限拆分**：GPTS 的功能可见权限与管理权限已拆分，`gpts.manage` 可单独控制创建、编辑和删除能力。
- **模块隔离**：管理员后台的模型配置、权限配置和功能开关已分别受 `models.manage`、`permissions.manage`、`feature_flags.manage` 控制。
- **模型收口**：`default_visible_models` 已开始控制主助手模型列表和默认回退选型，管理员改完后可直接影响聊天侧可见模型。
- **思考默认值**：`default_reasoning_enabled` 已开始接管主助手默认思考开关，前后端默认态保持一致。
- **主助手默认值**：主助手的运行时默认配置已统一收口到管理员 feature flags，模型可见性和默认思考不再只依赖静态内置配置。
- **默认模型切换**：`default_model` 已开始接管主助手默认模型选择，聊天选型和详情接口会统一读取运行时配置。
- **表单化配置**：管理员页新增“主助手默认配置”区域，默认模型、可见模型和默认思考开关不再依赖手填通用 JSON 开关。
- **一致性校验**：保存主助手默认配置时，会校验默认模型必须来自当前模型配置列表，且必须包含在默认可见模型中。
- **业务开关**：管理员页新增“业务开关”区域，支持直接启用或关闭 GPTs 功能，不再要求手动编辑 `gpts_feature_enabled`。
- **配置分层**：通用 feature flags 区继续保留，但更偏向低频、实验性或暂未产品化的配置项。
- **变更保护**：结构化配置区域和通用编辑器都新增未保存变更提示、重置按钮以及离开页面前提醒，降低误刷新、误关闭或误跳转导致的配置丢失。
- **保存体验**：管理员页保存和删除配置后改为按模块局部刷新，只单独更新审计区，减少整页数据重载带来的打断感。
- **默认数据种子**：当管理员模型、权限或功能开关表为空时，后端会自动用当前内置主助手模型、现有白名单和默认业务开关初始化一份基础配置，避免本地或新环境首次进入管理员页时出现空白状态。
- **布局收紧**：管理员配置页调整为更高信息密度的控制台布局，扩大内容宽度、压缩卡片留白并提升卡片列数，减少“一屏只能看到两个卡片”的情况。
- **架构分离**：`/admin` 继续留在同一个前端应用中，但已切换为独立工作区布局，不再复用聊天左侧菜单和输入区。
- **多页面后台**：管理员区已拆成模型、权限、功能开关、审计四个独立页面，左侧菜单改为真实路由导航，不再依赖单页长滚动。
- **会话历史收口**：聊天历史列表和历史详情开始以服务端会话表为主数据源，前端不再长期持久化整段会话内容到浏览器本地。
- **7 天兜底**：旧浏览器本地历史不再导入成功后立刻删除，而是保留 7 天本地兜底窗口，优先走服务端、缺失时再回退本地，平滑过渡到服务端历史。
- **服务端可观测**：前端会把“服务端已有多少历史、还有多少会话仍依赖本地兜底、哪些会话发生了本地回退”上报到后端日志，方便观察稳定性后再彻底清理本地数据。

## [2026.05.25] [v1.1.2] 发送拦截保留附件
- **交互修复**：空文本请求被前端拦截时，不再清空已上传附件。
- **提交逻辑**：输入区仅在真正进入发送流程后才清空文本和附件，避免拦截场景下误丢内容。
- **待发送预览**：上传图片后，输入区中的待发送附件恢复为缩略图展示。
- **尺寸微调**：将输入区图片缩略图调整为更紧凑的正方形裁切，保持与其它附件卡片一致的高度节奏。
- **展示修复**：恢复历史聊天记录中图片附件的缩略图展示，并支持点击放大预览。
- **类型补充**：历史附件列表补充 PPT/PPTX 类型标识，避免统一显示为普通文件。
- **识别增强**：按文件类型细化附件图标配色，提升 Word、Excel、PDF、PPT 等文档的辨识度。
- **发送约束**：输入区未填写正文时禁用发送按钮，减少无效点击和误触发提示。
- **状态修复**：点击制度助手等页面的预置问题后，发送按钮会立即按输入内容同步激活，不再错误保持置灰。
- **链路统一**：预置问题填充改为复用输入事件链路，确保输入框高度和发送状态与手动输入一致。
- **制度助手修复**：补齐 `chat_with_kernel_regulation` 的 `trace_recorder` 参数，避免制度问答助手请求启动时抛出参数错误。
- **统计修正**：浏览器刷新和后台标签页被浏览器丢弃后恢复时，不再上报为 `suspected_crash`，降低运行时稳定性统计误报。
- **统计修正**：运行时日志汇总上限从 500 条放宽到 5000 条，降低 heartbeat 覆盖异常事件导致的统计偏低。

## [2026.05.09] [v1.1.1] 助手置顶权限修正
- **权限修正**：更多助手白名单仅控制入口和管理页，不再隐藏已置顶助手。
- **默认置顶**：制度问答助手保持默认置顶，并禁止取消置顶。

## [2026.05.07] [v1.1.0] 附件格式扩展
- **格式扩展**：主 AI 助手新增 Markdown、CSV 和 PPTX 文件上传支持。
- **文本提取**：上传后的 Markdown、CSV 和 PPTX 附件可参与文档文本提取和对话分析。

## [2026.06.14] [v1.6.0] 主助手迁入我的智能体
- **主助手迁库**：将 `gptassistant` 迁移为数据库驱动的系统智能体，和制度助手、普通自定义智能体共用统一配置模型。
- **配置统一**：主助手、制度助手和自定义智能体统一支持在智能体配置中维护默认模型、可见模型和知识文件，不再依赖后台的主助手默认配置特例。
- **权限闭环**：系统智能体沿用 `owner/admin/viewer` ACL；白名单首位默认成为系统智能体 owner，其余白名单成员默认成为管理员，并可在“我的智能体”中继续授权和转让。
- **入口收口**：管理后台中的主助手默认配置改为迁移提示，实际修改入口统一收敛到“我的智能体”。

## [2026.05.07] 新建会话入口精简
- **侧边栏优化**：移除“新建会话”入口右侧的快捷键提示，减少视觉干扰。
- **对话框优化**：调整输入框外壳、底部控件布局与附件按钮样式，向企业版模板的 composer 视觉靠拢，并保留思考与附件能力。
- **细节调整**：在“添加附件”和“思考”控件之间增加竖线分隔，提升底部控件区的层次感。
- **工具带优化**：弱化“添加附件”和“思考”的按钮感，收成更轻的底部工具带，并保留右侧发送按钮为唯一主动作。
- **样式修正**：移除“思考”工具项中的重复图标，仅保留圆点状态标记。
- **视觉微调**：放大“添加附件”的加号入口，并将“思考”进一步收成更自然的文字工具项。
- **布局调整**：将“思考”从左侧工具区移动到右侧动作区，与发送按钮形成更清晰的轻重关系。

## [2026.05.07] 模型选择文案精简
- **界面精简**：移除顶部模型选择区域中的“AI助手”前缀，仅保留当前模型名称显示。

## [2026.04.29] [v1.0.3] 聊天界面交互细节优化
- **侧边栏优化**：细化历史会话 hover、选中、操作菜单、用户菜单和移动端长按操作表现。
- **主区优化**：调整模型选择器、新建会话页标题和默认 mock logo 展示，制度问答助手保留专属欢迎文案。

## [2026.04.28] [v1.0.2] 聊天界面视觉基线优化
- **视觉优化**：统一聊天主页字体、字号和正文行高，向新模板的阅读节奏靠拢。
- **层级调整**：调整侧边栏、顶部栏、消息卡片和输入区的浅色视觉层级，恢复主面板与侧边栏的区分。

## [2026.04.23] [v1.0.1] 版本入口与变更记录
- **交互优化**：侧边栏 Logo 区增加版本号入口，当前版本展示为 `v1.0.1`。
- **功能记录**：点击版本号可打开功能变更记录悬浮窗，支持从新到旧滚动查看和关闭。
- **规范补充**：明确版本号升级规则，后续变更需同步评估版本号更新。

## [2026.04] [v0.9.0] 诊断与链路优化
- **全链路监控**：引入链路诊断工具、Trace 查看器及模型原始输入日志，提升问题排查效率。
- **架构升级**：GPT 助手 `chat-v2` 流程重构，去掉 planner，统一采用 `tool-first` 附件链路，支持模型按需调用 `document_read_text`。
- **配置增强**：支持通过环境变量动态配置模型 ID；增加 GPTS 访问白名单机制。
- **体验优化**：提高文件上传大小限制，增加工具调用进度和附件预处理状态的实时展示。

## [2026.03] [v0.8.0] 架构重构与交互升级
- **核心内核**：引入 `llm kernel` 基础架构，解耦模型处理逻辑。
- **附件工具流**：上线 `gptassistant v2` 附件处理流程，大幅优化文档工具的响应速度。
- **滚动体验**：重构聊天窗口滚动容器，实现独立的自动滚动逻辑，解决流式输出时的阅读干扰问题。
- **UI 优化**：美化附件展示样式和侧边栏交互。
- **兼容性**：优化思考模式（Thinking）在各兼容网关上的格式输出。

## [2026.02] 扩展与统计
- **接入扩展**：增加 Claude 模型接入文档；更新 `model_tool` 功能。
- **用量统计**：上线项目维度用量统计功能，支持多模型占比分析。

## [2026.01] [v0.7.0] LLM Platform 上线
- **管理门户**：新增 `platform` 页面，支持管理 API Key 和 Token 消耗监控。
- **部署自动化**：增加一键部署脚本及构建流程优化。
- **安全增强**：敏感配置（API Key 等）在前端进行掩码处理。

## [2025.12] [v0.6.1] 细节打磨
- **性能优化**：前端项目全面迁移至 Vite，显著提升开发和编译速度。
- **逻辑修复**：修复思考模式（think）标签在某些流式场景下的闭合问题。

## [2025.11] [v0.6.0] 功能完善
- **功能增强**：引入待办事项（TODO）等辅助开发功能。
- **问题修复**：解决数据大屏时间筛选器在特定时区下的失效问题。

## [2025.10] [v0.5.0] 架构重构与数据大屏
- **多端拆分**：项目大重构，划分为 `assistant-web` (主应用)、`assistant-dashboard` (数据大屏) 和 `assistant-metrics-api` (统计后端)。
- **数据可视化**：上线 GPT 助手核心指标总览页面，支持用户数、请求数及热门模型排名统计。
- **模型记忆**：实现基于会话的模型偏好记忆功能，用户返回旧会话时自动切换至上次使用的模型。
- **上传策略**：实现模型相关的上传文件类型限制（如 VL 模型限制图片，Instruct 模型限制文档）。
- **交互稳健性**：优化 WebSocket 连接稳定性；修复 VPN 环境下跨服务调用的响应问题。

## [2025.09] [v0.4.0] GPTs 平台化
- **管理功能**：上线 GPTs 创建与管理 API，支持用户自定义系统提示词（Prompt）和示例问题。
- **权限控制**：增加 GPTS 访问 white list，非授权用户将隐藏管理入口。
- **国际化**：初步完成 GPTs 相关页面的中英文国际化适配。
- **数据持久化**：后端存储由内存/文件迁移至 SQLite 数据库，确保配置不丢失。

## [2025.08] [v0.3.0] 可视化增强
- **可视化增强**：引入 Markdown 中的 ECharts 渲染支持，使模型能直接生成交互式图表。
- **导航优化**：新增 GPTs 独立视图和侧边栏固定/取消固定功能。

## 早期记录 [v0.1.0]
- **25年7月**：上线多个纪委智能助手，上线模型切换功能。
- **25年6月**：非现场问答助手优化，支持中止回答功能，精简代码，修复登录 Session 过期问题。
- **25年5月**：接入 SSO 单点登录，支持三端同步展示。
- **25年4月**：核心接口由 Dify 迁移至自研后端服务。
- **25年3月**：制度问答助手上线，支持多轮思考和工具调用。
- **25年2月**：接入 DeepSeek 模型，实现思考模式展示。
- **24年6月**：GPT 助手 1.0 版本正式上线。
