# TODO

- **前后端协议重构**：优化 GPT 助手的通信协议，提升数据传输效率。
- **能力优化**：问答智能，文件智能。重点用通用的能力解决具体问题。
- **能力扩展**：MCP智能，Skills智能，GPTs，多智能体协同。
- **模型能力配置本地化**：主聊天助手的模型能力判断（如是否支持思考、图片输入、工具调用、思考参数格式）应由项目本地配置管理，不再依赖 `OPENAI_BASE_URL/v1/models` 的远端扩展字段。理由：OpenAI 兼容 `/models` 接口通常不保证返回这些扩展能力字段，远端不可用或字段缺失会导致思考/图片等能力被误判关闭；模型能力也是管理员侧开放策略，需要可控、可审计、可回滚。改造方案：在本地 GPT 模型配置中显式维护 `supports_reasoning`、`supports_native_image_input`、`supports_tool_calling`、`compat.reasoning_parameter_format` 等字段，移除或禁用远端模型元数据补充逻辑，运行时只认项目本地配置。
- **LLM Platform 模块拆分**：将 LLM Platform 从当前项目中拆分为独立前端和后端模块，明确平台控制台、网关管理、用量指标、访问控制等边界，降低与 assistant-web、assistant-bff 的耦合，便于独立部署、演进和权限治理。
- **流式会话内存治理**：当前已对流式输出期间的会话状态更新做节流，但后续仍应继续收敛前端内存占用。建议方向：流式回复先进入 `useRef` buffer 并按固定频率渲染，结束后一次性落 Redux；会话列表摘要和会话详情状态拆分，避免 Redux 长期持有大量历史详情；旧历史和附件状态做轻量化清洗，确保前端状态只保留 fileId、文件名、mimeType、size 等元信息，不保存 base64 或文件正文。

## 文件上传与附件链路后续治理

### 高优先级代码治理

- **原生图片历史轻量化**：原生图片当前会完整读取并转换为 Base64，随后随用户消息持久化到会话历史，并可能在后续聊天中重复发送给模型；上下文预算目前也未统计图片数据。后续应只在当前请求中临时构造图片输入，历史记录仅保留附件引用和必要元信息，并将图片数量、原始字节数或估算 token 纳入上下文预算。
- **Trace 权限与附件数据脱敏**：开启 Trace 后，聊天诊断接口需要增加登录和管理员权限校验；Trace 事件不得保存图片 Base64、文件正文或其他附件原始内容，只记录 fileId、类型、大小、数量和必要摘要。
- **上传解析前并发保护**：当前请求体大小限制可以阻止超大请求，但上传信号量在 multipart 解析完成后才生效。后续应在网关和应用入口增加按用户/IP 的请求速率、并发数、连接数和排队时长限制，避免大量合法大小上传占满临时磁盘和连接资源。
- **对象存储调用超时**：为 MinIO 上传、下载和删除操作配置明确的连接、读取和总时长超时，并补充超时后的重试、失败状态和告警策略，避免对象存储卡顿长期占满上传槽。
- **文档解析资源隔离**：解析子进程已有执行超时，但仍需要通过容器、cgroup 或独立解析服务限制内存、CPU、网络访问、文件系统权限和环境变量可见范围，降低复杂或恶意文档导致 OOM 或扩大攻击面的风险。
- **附件间接 Prompt Injection 防护**：上传文档内容属于不可信数据。后续应明确附件内容与系统指令的信任边界，对具备业务工具或写操作能力的助手增加工具授权、人工确认、只读默认和敏感操作阻断，避免文档内容诱导模型执行非预期工具调用。

### 中优先级一致性治理

- **原生图片预处理异步化**：图片读取和 Base64 编码当前在异步聊天路径中同步执行。后续应移入受控线程池或流式输入链路，并增加附件预处理并发限制，降低多图片并发聊天时的事件循环阻塞和内存峰值。
- **聊天阶段附件策略复核**：文件上传时会按模型校验附件类型，但文件映射未记录上传策略，聊天阶段只校验归属、数量和总大小。后续应明确附件类型规则是体验提示还是安全边界；若属于安全或能力边界，应在实际聊天使用时根据目标模型和目标助手重新校验。
- **孤儿对象巡检与回收**：常规异常和客户端取消已有清理，但进程强杀、节点断电或对象写入后数据库提交前崩溃仍可能留下无映射对象。后续应增加对象存储与文件映射的定期对账、隔离期和安全回收流程。
- **附件使用中保护**：过期清理可能与下载、解析或模型发送并发发生。后续可增加短期租约、使用中标记或删除前二次确认，降低临近过期时间时的偶发 404 和解析失败。
- **用户主动删除附件**：当前移除待发送附件只会更新前端状态，服务端对象仍等待保留期清理。后续可提供带归属校验的附件删除接口，并处理仍被会话引用时的行为。
- **恶意文件检测能力**：现有扩展名、基础签名和 Office 压缩包检查不能替代病毒或恶意内容检测。根据生产数据敏感性评估接入杀毒扫描、沙箱分析或上传隔离区。

### 生产部署与验证

- **多节点存储约束**：多节点部署必须使用共享 MinIO 或共享文件目录；Postgres 共享但附件仍使用节点本地 filesystem 时，负载均衡到其他节点会出现附件偶发不存在。
- **真实环境压力测试**：使用生产同构的 Postgres、MinIO、反向代理和多节点部署，覆盖并发上传、慢速上传、客户端取消、MinIO 超时、节点重启、批量图片聊天、复杂文档解析和过期清理并发场景。
- **容量与异常监控**：增加上传排队数、multipart 临时目录容量、上传/解析并发数、MinIO 延迟与错误率、孤儿对象数量、附件缓存容量、解析超时/OOM 和用户配额使用率监控及告警。
